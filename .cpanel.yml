---
deployment:
  tasks:
    - export DEPLOYPATH=$HOME/public_html/
    - /usr/bin/rsync -av --exclude=.git --exclude=.gitignore --exclude=.cpanel.yml --exclude=.env --exclude=CLAUDE.md --exclude=.DS_Store ./ $DEPLOYPATH
